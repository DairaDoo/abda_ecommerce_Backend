export class AuthPayLoadDto {
    // Definición de la clase AuthPayLoadDto que representa el payload de autenticación.

    // Propiedad que almacena el nombre de usuario para la autenticación.
    username: string;

    // Propiedad que almacena la contraseña para la autenticación.
    password: string;
}
